package models;

public class AnimalAttributes {
    public enum Gender {
        MALE, FEMALE
    }

    public enum Species {
        DOG, CAT
    }

    public enum Size {
        SMALL, MEDIUM, LARGE
    }
}